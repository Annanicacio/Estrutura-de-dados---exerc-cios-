// Métodos Inserir no incio, mostrar lista e remover do inicio. 

//exercício 3. Inserir no início.

//Implementar inserirNoInicio(): Crie um método public void inserirNoInicio(int
//dado) que crie um novo nó com o dado informado e o adicione no início da lista.
//Lembre-se de atualizar a referência primeiro:


public void inserirNoInicio(int dado) {
    No novoNo = new No(dado);    
    novoNo.proximo = primeiro;   
    primeiro = novoNo;           
}


//exercício 4. Mostrar lista.

//Implementar mostrarLista(): Crie um método public void mostrarLista() que
//percorra a lista desde o início e imprima o dado de cada nó no console, no formato
//[dado1] -> [dado2] -> null.

public void mostrarLista() {
    No atual = primeiro;                   
    
    while (atual != null) {               
        System.out.print("[" + atual.dado + "] -> ");  
        atual = atual.proximo;             
    }
    
    System.out.println("null");           
}


//exercício 5. Remover do Inicio.

//Implementar removerDoInicio(): Crie um método public No removerDoInicio() que
//remova o primeiro nó da lista e o retorne. Se a lista estiver vazia, deve retornar null

public class ListaLigada {
    private No primeiro;
    
    // VERSÃO MAIS COMUM NO MUNDO REAL: void
    public void removerDoInicio() {
        if (primeiro != null) {
            primeiro = primeiro.proximo;
        }
    }
    
    // VERSÃO QUE RETORNA VALOR (também comum)
    public Integer removerDoInicioERetornarValor() {
        if (primeiro == null) {
            return null;
        }
        int valor = primeiro.dado;
        primeiro = primeiro.proximo;
        return valor;
    }
    
    // VERSÃO DO EXERCÍCIO (retorna nó completo)
    public No removerDoInicioERetornarNo() {
        if (primeiro == null) {
            return null;
        }
        No removido = primeiro;
        primeiro = primeiro.proximo;
        return removido;
    }
}
